%nav.navbar.navbar-inverse
  .container-fluid
    .navbar-header
      %a.navbar-brand{href: "#"}
        Journal Officiel
    %ul.nav.navbar-nav.navbar-right
      - if user_signed_in?
        %li.dropdown
          %a.dropdown-toggle{'data-toggle' => 'dropdown', href: '#'}
            = current_user.email
            %span.glyphicon.glyphicon-chevron-down{'aria-hidden' => "true"}
          %ul.dropdown-menu{role: 'menu'}
            %li= link_to 'Mon profil', user_path(current_user)
            %li= link_to 'Modifier ses infos de connexion', edit_user_registration_path
            %li= link_to 'Déconnexion', destroy_user_session_path, :method=>'delete'
      - else
        %li= link_to "Sign in", new_user_session_path
        %li= link_to "Sign up", new_user_registration_path
