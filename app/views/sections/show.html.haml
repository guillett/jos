%h1=@section.title
- @articles.each do |article|
  %h2
    %a{href: "/articles/#{article.id_article_origin}"}="#{article.nature}  #{article.number}"
  %p= raw(article.text)
  -unless article.nota.blank?
    %h3= "Nota "
    %p= raw(article.nota)

= raw display_summary(@sub_sections, 'accordion')